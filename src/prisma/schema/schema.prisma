generator client {
  provider     = "prisma-client"
  output       = "../client"
  moduleFormat = "esm"
  previewFeatures = ["driverAdapters"]
}

datasource postgresql {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
