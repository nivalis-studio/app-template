generator client {
  provider = "prisma-client"
  output   = "client"
}

datasource postgresql {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Temp {
  id Int @id @default(autoincrement())
}
